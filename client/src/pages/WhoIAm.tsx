/* Who I Am: personal story architecture, intentionally empty until Haripriya provides the wording. */
import { EmptyState } from "../App";
const sections = ["MY STORY", "HOW I THINK", "HOW I WORK", "WHAT DRIVES ME", "WHAT I CARE ABOUT"];
export default function WhoIAm() { return <section className="page container who-page"><div className="page-intro"><span className="eyebrow">02 / Exploration</span><h1>WHO <em>I AM.</em></h1><p>This is not the résumé version. This is the person behind the work.</p></div><div className="story-grid">{sections.map((section, i) => <section className={i === 0 ? "story-block wide" : "story-block"} key={section}><span className="project-number">0{i + 1}</span><h2>{section}</h2><EmptyState label="CONTENT TO BE PROVIDED" detail="The real writing will live here." /></section>)}</div><div className="persona-section"><span className="eyebrow">The person behind the platforms</span><h2>THREE VERSIONS<br /><em>OF ME.</em></h2><div className="persona-grid">{[1, 2, 3].map((item) => <div className="persona-card" key={item}><span>PERSONA 0{item}</span><h3>[TO BE PROVIDED]</h3><EmptyState label="PLATFORM / NAME / IDENTITY" detail="Purpose, themes, visual identity, and link to be added." /></div>)}</div></div></section>; }

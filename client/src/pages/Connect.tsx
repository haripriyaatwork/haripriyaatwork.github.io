/* Connect: direct, warm, and deliberately editable until real links and contact details are supplied. */
import { useState } from "react";
import { ArrowUpRight } from "lucide-react";
import { EmptyState } from "../App";
export default function Connect() { const [sent, setSent] = useState(false); return <section className="page container connect-page"><div className="page-intro"><span className="eyebrow">07 / Proof + exploration</span><h1>LET’S<br /><em>TALK.</em></h1><p>For work. For ideas. For research. For writing. For something I haven’t thought of yet.</p></div><div className="connect-grid"><div className="connect-links"><span className="eyebrow">Find me where the links are</span><div><EmptyState label="EMAIL TO BE PROVIDED" /><EmptyState label="LINKEDIN TO BE PROVIDED" /><EmptyState label="SUBSTACK TO BE PROVIDED" /><EmptyState label="INSTAGRAM TO BE PROVIDED" /><EmptyState label="X TO BE PROVIDED" /></div></div><form className="connect-form" onSubmit={(e) => { e.preventDefault(); setSent(true); }}><span className="eyebrow">Start a thread</span><label>Name<input required placeholder="Your name" /></label><label>Email<input required type="email" placeholder="you@example.com" /></label><label>What’s on your mind?<textarea required rows={5} placeholder="A brief, an idea, a question..." /></label><button className="button button-rose">{sent ? "Noted" : "Send the signal"} <ArrowUpRight size={15} /></button>{sent && <p className="form-success">[FORM CONNECTION TO BE ADDED]</p>}</form></div></section>; }

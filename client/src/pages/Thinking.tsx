/* Thinking: a quieter, more intimate archive separate from project work. */
import { EmptyState } from "../App";
const sections = ["ESSAYS", "OPINIONS", "RESEARCH", "LINKEDIN WRITING", "SUBSTACK", "POETRY", "COMMUNICATION IDEAS", "OBSERVATIONS", "PERSONAL THINKING"];
export default function Thinking() { return <section className="page container thinking-page"><div className="page-intro"><span className="eyebrow">04 / Exploration</span><h1>THINGS I<br /><em>THINK ABOUT.</em></h1><p>Work is what I make for a brief. Thinking is what stays with me after.</p></div><div className="thinking-grid">{sections.map((section, i) => <article className={i === 0 ? "thinking-card featured" : "thinking-card"} key={section}><span className="project-number">0{i + 1}</span><h2>{section}</h2><EmptyState label="WRITING TO BE ADDED" detail="Title, excerpt, date, tags, and link will appear here." /></article>)}</div><div className="poetry-mode"><span className="eyebrow">A different kind of archive</span><h2>POETRY</h2><p>More intimate. More editorial. More minimal.</p><EmptyState label="POEMS TO BE ADDED" /></div></section>; }
